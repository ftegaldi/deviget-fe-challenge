import { CardContainer } from './styled';

const PostCard = ({ postData }) => {
  // const { id, title, author, created_at, thumbnail, url, comments } = postData;
  return (
    <CardContainer>
      <div>this is the inside of the card</div>
    </CardContainer>
  );
};

export default PostCard;
